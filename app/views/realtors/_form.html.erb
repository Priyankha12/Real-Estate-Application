<%= form_with(model: realtor, local: true) do |form| %>
<!--% form_for(@realtor) do |form| %-->
  <% if @realtor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(realtor.errors.count, "error") %> prohibited this realtor from being saved:</h2>

      <ul>
      <% @realtor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>


  <!--%= form.hidden_field :encrypted_password, value: @realtor.password %-->



  <div class="field">
    <%= form.label :real_estate_company_id %>
    <%= select("realtor", "real_estate_company_id", RealEstateCompany.all.collect{ |c| [c.name, c.id] }) %>
  </div>

  <%= link_to 'Create new real estate company', new_real_estate_company_path %></td>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
